# Commander (core)
pycryptodome>=3.19  # antes cryptography (requeria Rust; historial de romperse en Termux)

# SourceSeal Tactical (opcional pero recomendado)
fastapi>=0.104.0
uvicorn>=0.24.0
aiohttp>=3.9.0
httpx>=0.25.0
pydantic>=2.0.0
